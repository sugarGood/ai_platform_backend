<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 在引入 Boot 默认配置前设置控制台格式，避免与默认 Pattern 冲突 -->
    <springProfile name="prod">
        <property name="CONSOLE_LOG_PATTERN"
                  value="%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%15.15thread] %-40.40logger{39} : %msg%n%wEx"/>
    </springProfile>
    <springProfile name="!prod">
        <property name="CONSOLE_LOG_PATTERN"
                  value="%d{yyyy-MM-dd HH:mm:ss.SSS} %highlight(%-5level) [%15.15thread] %cyan(%-40.40logger{39}) : %msg%n%wEx"/>
    </springProfile>

    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <!-- 保留启动横幅与 Boot 关键信息 -->
    <logger name="org.springframework.boot" level="INFO"/>
    <logger name="org.springframework" level="WARN"/>

    <logger name="org.apache.catalina" level="WARN"/>
    <logger name="org.apache.coyote" level="WARN"/>
    <logger name="org.apache.tomcat" level="WARN"/>

    <logger name="com.zaxxer.hikari" level="WARN"/>
    <logger name="com.baomidou.mybatisplus" level="WARN"/>
    <logger name="org.apache.ibatis" level="WARN"/>
    <logger name="org.mybatis" level="WARN"/>

    <logger name="io.lettuce" level="WARN"/>
    <logger name="io.netty" level="WARN"/>
    <logger name="reactor.netty" level="WARN"/>

    <logger name="org.springdoc" level="WARN"/>
    <logger name="org.springframework.web.servlet.mvc" level="WARN"/>

    <logger name="cn.dev33.satoken" level="WARN"/>

    <logger name="io.minio" level="WARN"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
